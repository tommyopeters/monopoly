let leftRow = document.querySelector(".left-row");
let leftRowContainers = document.querySelectorAll(".left-row .space");

// console.log(leftRow.offsetHeight);

// leftRowContainers.forEach(el => {
//   el.style.width = leftRow.offsetHeight / 9 + "px";
//   console.log(leftRow.offsetHeight / 9);
// });
